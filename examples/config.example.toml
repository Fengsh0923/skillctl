# Minimal skillctl config — copy to ~/.skillctl/config.toml and edit.

[[sources]]
status = "active"
root = "~/.claude/skills"
glob = "*/SKILL.md"

# ~/.claude/commands/ holds slash-command definitions, not skills —
# scanning it pollutes the catalog, so it is intentionally omitted.

[budget]
description_budget = 15000
